{
  "name": "tally-app",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "dev": "turbo run dev --filter=@tally/web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "pnpm": {
    "overrides": {
      "eslint-plugin-react-hooks": "7.0.1"
    }
  }
}
